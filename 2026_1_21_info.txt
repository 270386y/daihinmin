<2026.1.21今回の進捗>
前回のデータで集計した試合数が1000で差異が分かりにくかった
→前回のデータを10000回で実行

・現在の変更で起きた変化の現状をはっきりさせること
・今後の動きの確立
を目標に集計を行う。

(注意)今回コードはいじっておりません。
→前回コードを弄った場所の共有不足+膨大なデータの暴力+他リソースで時間不足
の複数の原因が重なり、下手に弄れなかった

<データ集計結果>

0, Switch2+
1, switch2_test
2, default
3, default
4, default

--------- total point
38865 38800 24052 24043 24240
All games are overed
<備考>
途中経過でswitch2_testがSwitch2+より点数が高くなっていた場面は伺えなかった

0, switch2_test
1, Switch2+
2, Switch2+
3, Switch2+
4, Switch2+

--------- total point
29960 29745 30274 29824 30197
All games are overed

--------- total point
29460 30030 29934 30257 30319
All games are overed

<備考>
1回目
序盤はswitch2_testは点数が高かったものの、その後最下位に転落。
終盤で逆転し、2, 3位まで浮上。

2回目
中盤から失速気味、終盤かなり失速していたように見受けられる。

<前回の変更による結論>
一つの戦略に追加したものが結果に与えた影響は少ないと考える。
→単体で作成し、まずdefaultと戦わせ、明らかに強くなるようにするのが
最初の目標にするべきであった（最初から組み込むのはよくない）

なお、そうしたとしても
・追加した戦略単体のパワーが既に実装されている戦略単体のパワーと
同等かそれ以上でないと選択されず、変わらない可能性がある
(これが今回の原因の根幹説はある)
・既存の戦略に何かしらコードを追加して新しい戦略として実装したとしても
既存の戦略と動きの根幹はあまり変わらないため、役割が被ってしまう
可能性もある
→戦略を「追加する」方法をとるのであれば、一からアルゴリズムを考え
実装するべきなのでは？
or
少なくともパラメータは変えなければ新戦略としての意味がないのでは？

一つの戦略を編集しただけ
→Switch2+の特性として、戦略の一つを変えてしまうとバランスが乱れ
途中から選ぶ戦略を絞った際に変えた戦略が選ばれなかった可能性あり
→デバックでどの戦略が選択されたかを出力させるべき
(なお、コードが3万行であるためどこに書けばよいのか追い切れていない
→Switch2+のコード内の理解が追い付いていない可能性(もしかしたら私だけかも))
→次回までにコードの理解深めておきます(ごめんなさいに尽きる)
→[1/21 10:40]今解決しました（後述

3万行のコードはあくまでclient.cに使用するためのメソッドを置いているdaihinmin.cのファイルのことで、
戦略云々の話はclient.cにあった模様。
前回記述していた内容を見たところ、senjutu=3で戦術を追加した戦術のみに固定し、
戦術選択のコードを全てコメントアウトしていた模様
→使われていない事象は存在しないため、この仮説は排除できる
↓以下、そう言えるであろう理由のコード

    //:::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::
    //使用する戦術の決定

    senjutu = 3;　←ここで固定

    /*if (game_count % 100 == 1) {　　←ここからコメントアウト
      for (i = 0; i < 100; i++) {
        values[i] = i;
      }
      shuffle(values, size);
    }
    tyuusen = values[game_count % 100];
   
    if (game_count <= 1000) 
    {
      if (tyuusen < 17) {
        senjutu = 0;
      } else if ((17 <= tyuusen) && (tyuusen < 34)) {
        senjutu = 1;
      } else if ((34 <= tyuusen) && (tyuusen < 51)) {
        senjutu = 2;
      } else if ((51 <= tyuusen) && (tyuusen < 68)) {
        senjutu = 3;
      } else if ((68 <= tyuusen) && (tyuusen < 84)) {
        senjutu = 4;
      } else {
        senjutu = 5;
      }
    }
    
    ………

不安で、実際にclient.cの試合をループさせる処理の最初と最後に以下のコードを打ち込んで1000試合実行
(なお、最初はfirstSenjutu3Count, 最後はlastSenjutu3Countとして別々の変数にし、被らないようにしている)

各ゲームの最初
if (senjutu == 3) {
    firstSenjutu3Count++;
    fprintf(stderr, "[start senjutu3 count] %d \n", firstSenjutu3Count);
}

各ゲームの最後
if (senjutu == 3) {
    lastSenjutu3Count++;
    fprintf(stderr, "[finish senjutu3 count] %d \n", lastSenjutu3Count);
}

実行結果は以下の通り(最後のログのみ)
[start senjutu3 count] 1000
[finish senjutu3 count] 1000
→試合回数と一致
→途中で戦略の値が塗り替えられていなければずっと追加変更した戦略のみ使用していることが確定

Switch2+から追加したコードの内容（ここ以外変わっていない）

if(senjutu == 3){
    // 1つ上の順位の人を狙う処理

    int target_id = -1; // ターゲットのID
    int my_rank_now = ss.old_rank[my_playernum]; // 自分の順位

    // 1つ上の順位の人を探す
    for(i = 0; i < 5; i++){
        if(ss.old_rank[i] == my_rank_now - 1){
            target_id = i;
            break;
        }
    }
    // 場にカードが出ていて(onset==0)、かつ 直前に出したのがターゲットなら
    if (state.onset == 0 && last_playernum == target_id && target_id != -1) {
        // 評価値を上げて、勝てるカードがあれば無理やり出させる
        attack_flag = 1;

        for (j = 0; j < 3; j++) {     // j:進行度(序盤中盤終盤)
            for (k = 0; k < 4; k++) { // k:役の種類
                 tes[j][k][2] += 100; 
            }
        }
    }
}

1位の時はtarget_id = -1のまま
→攻撃対象がないので永久に攻撃フラグが立たず、攻撃的にならない（？）

戦術3がどんな内容なのかをちゃんと把握しなければ言った通り変わんない（重要）
→元の戦術3のみと対戦結果を比べてみると強くなったかわかるかも…？

<追加コメント>
10000試合の実行でかかる時間は約5分程(orそれよりもかからない位)
→この長さを超えてしまうとライト級は✕か？

環境構築がどうやらうまくいっていなくて計5時間追加で潰れました(ガチ)
→どうやらスペック足りないとグラフ推移は重すぎて無理かも…
100試合目経たずにグラフ画面等に表示が追い付かなくなりサーバが落ちて
一生実行できなかったので今回の実行は表示をプロンプト内のみに留めました。
(今回使った実機がこのノートPC、前回は相方のゲーミングPC。どちらも仮想Linux)
→次回までに自宅のデスクに環境構築を行い原因を探っておきます
(遅いって言われても何も言い返せない、こうなると思ってなかった)

ソフトウェア工学の方で成果の進捗が前回の結果の詳細のみになったことを
お許しください。

データを言葉にして残すのは大切ですね(今更)
これからもこの進捗を文字に残すという形態を続けていこうと思います。

<輪講後の教授からの意見>
まずはSwitch2+に追加として組み込んでみて差異を確認してみるのはどうかね